#include "schatz.h"

schatz::schatz(void)
{
    // Schatzkonstruktor
    eingesammelt = false;
    schaetze??(i??)->s->zeigeSchritt(0);        // Inklusive Trigraphs ;)
    schaetze??(i??)->s->sprite.setOrigin(0,0);
}


schatz::~schatz(void)
{
    // Speicher freigeben!
    delete s;
}
